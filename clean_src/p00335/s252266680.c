//	http://judge.u-aizu.ac.jp/onlinejudge/review.jsp?rid=2415733#1

#include <stdio.h>

#define Rep(i,N) for(int i = 0;i < N;i++)
 
int main( void ){

	int n;			// ????????±??????????????° N(3???N???5000)
	int d[ 5000 ];	// ?????????????????§?????????????£????????????° pi(0???pi???3)
	int w[ 5000 ];	// 
	int cnt;		// ?£????????????°
	int total;		//
	int i, j;

	while ( 1 ) {
		scanf( "%d", &n );	// ???????????±??????????????°????????\???

		if ( n == 0 ) {
			break;			// ??\???????????????
		}

		for ( i = 0 ; i < n ; i++ ) {
			scanf( "%d", &d[ i ] );
			w[ i ] = d[ i ];
		}

		cnt = 5000 * 3 * 2;		// ??¨????????????????£????????????°????????§???????¨????
		
		for ( j = 0 ; j < w[ 0 ] + 1 ; j++ ) {
			total = 0;
			
			w[ 0 ] -= j;
			
			total += j;
			
			for ( i = 0 ; i < n - 1 ; i++ ) {
				total += ( w[ i ] * 2 );
				w[ i + 1 ] -= w[ i ];
				w[ i ] = 0;
				
				if ( w[ i + 1 ] < 0) {
					w[i + 1] = 0;
				}
			}
			
			total += w[ n - 1 ];
			
			if ( cnt > total ) {
				cnt = total;
			}
			
			// ????\???¨????????° w ??? d ??????????????????
			for ( i = 0 ; i < n ; i++ ) {
				w[ i ] = d[ i ];
			}
		}
		
		printf( "%d\n", cnt );
		
		break;
	}
	return 0;
}