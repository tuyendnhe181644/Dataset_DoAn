#include<stdio.h>

int main(){

    int N = 0;//??\?????°(?????°)
	int D = 0;//??????
    int i = 0;//??????????????°
	int now_d = 0;//????????°
	int now_s = 0;//?????¨?????????
    int now_f = 0;//?????¨????????°(0????????????????????±???)
	int f_diff = 0;//f-now_f
	long long total_s = 0;//?????????????????????
    int s[101010] = {0};//??\????§???°
    int f[101010] = {0};//??\????????°
    
    scanf("%d %d",&N,&D);//??\?????°??¨????????\???
    for(i=0;i<N;i++){//??\???
        scanf("%d %d",&s[i],&f[i]);
        f[i] -= 1;//?????°???0????????????????????£
    }
    for(i=0;i<N;i++){
        f_diff = f[i] - now_f;//?????°??????
	    if(f_diff < 0) f_diff *= (-1);//?????°???????????¶?????????
        if((s[i] - now_s) >= (f[i] + now_f)){//????????????????£????????????????
            total_s += 1LL * now_f * now_d;//??????
			now_s = s[i];
			now_f = f[i];
			now_d = 1;
            continue;
		}
        if(((s[i] - now_s) >= f_diff)&&(now_d < D)){//???????????????????????????????????????????¶??????§????????????
            total_s += 1LL * (s[i] - now_s) * now_d;
			now_s = s[i];
			now_f = f[i];
			now_d++;
		}
        else{ 
            printf("-1\n");
            return 0;
        }
    }
    total_s += 1LL * now_f * now_d;//??????????????????????¨?
	printf("%lld\n",total_s);
    return 0;
}