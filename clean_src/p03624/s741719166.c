#include<stdio.h>

int main(void) {
	char s[100000];
	scanf("%s", s);
	int i;
	for (i = 0; s[i] != '\0'; i++) ;
	int j;
	for (j = 0; j < i; j++) if (s[j] == 'a') break;
	if (j == i) {
		printf("a");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'b') break;
	if (j == i) {
		printf("b");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'c') break;
	if (j == i) {
		printf("c");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'd') break;
	if (j == i) {
		printf("d");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'e') break;
	if (j == i) {
		printf("e");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'f') break;
	if (j == i) {
		printf("f");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'g') break;
	if (j == i) {
		printf("g");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'h') break;
	if (j == i) {
		printf("h");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'i') break;
	if (j == i) {
		printf("i");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'j') break;
	if (j == i) {
		printf("j");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'k') break;
	if (j == i) {
		printf("k");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'l') break;
	if (j == i) {
		printf("l");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'm') break;
	if (j == i) {
		printf("m");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'n') break;
	if (j == i) {
		printf("n");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'o') break;
	if (j == i) {
		printf("o");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'p') break;
	if (j == i) {
		printf("p");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'q') break;
	if (j == i) {
		printf("q");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'r') break;
	if (j == i) {
		printf("r");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 's') break;
	if (j == i) {
		printf("s");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 't') break;
	if (j == i) {
		printf("t");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'u') break;
	if (j == i) {
		printf("u");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'v') break;
	if (j == i) {
		printf("v");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'w') break;
	if (j == i) {
		printf("w");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'x') break;
	if (j == i) {
		printf("x");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'y') break;
	if (j == i) {
		printf("y");
		return 0;
	}
	for (j = 0; j < i; j++) if (s[j] == 'z') break;
	if (j == i) {
		printf("z");
		return 0;
	}
	printf("None");
	return 0;
}